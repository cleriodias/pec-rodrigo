<IfModule mod_rewrite.c>
    RewriteEngine On

    # Mantem o acesso direto a arquivos e pastas reais.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
